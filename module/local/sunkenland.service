[Unit]
Description=Sunkenland Service
Wants=network-online.target
After=syslog.target network.target nss-lookup.target network-online.target

[Install]
WantedBy=multi-user.target

[Service]
Type=simple
Restart=on-failure
RestartSec=10
User=${username}
Group=${username}
WorkingDirectory=/home/${username}/sunkenland
ExecStartPre=/home/${username}/steam/steamcmd.sh +force_install_dir /home/${username}/sunkenland +login ${steam_username} ${steam_password} +app_update ${steam_app_id} validate +quit
ExecStart=/home/${username}/sunkenland/bootstrap_sunkenland.sh
KillSignal=SIGINT
ExecStopPost=/home/${username}/sunkenland/backup_sunkenland.sh
